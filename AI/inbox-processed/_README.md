# inbox-processed

When Claude processes the inbox, the *original* inbox items move here so you can see what Claude classified, where it filed it, and undo if needed.

Filename pattern: `YYYY-MM-DD-[original-filename].md` with a frontmatter line `processed-to: <destination-path>`.
